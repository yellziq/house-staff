import type { ReactElement } from 'react';
import type { PropsWithChildren } from 'react';

const DashboardLayout = ({ children }: PropsWithChildren): ReactElement => <>{children}</>;

export default DashboardLayout;
